# === Dépendances ML (entraînement du modèle) — OPTIONNELLES ===
# Le serveur API n'en a PAS besoin en mode règles. Installe-les seulement pour
# entraîner un modèle :  pip install -r requirements-ml.txt
scikit-learn>=1.4,<2.0
joblib>=1.3,<2.0
pandas>=2.1,<3.0
