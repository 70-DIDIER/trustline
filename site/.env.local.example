# Copiez en .env.local — défauts déjà corrects pour le backend Django en local.

NEXT_PUBLIC_API_URL=http://127.0.0.1:8000/api
NEXT_PUBLIC_SITE_URL=http://localhost:3000

# Canaux — /telecharger (facultatif, désactive proprement la carte si absent)
NEXT_PUBLIC_EXTENSION_URL=/trustline-extension.zip
NEXT_PUBLIC_ANDROID_APK_URL=
NEXT_PUBLIC_USSD_CODE=
NEXT_PUBLIC_SMS_NUMBER=
